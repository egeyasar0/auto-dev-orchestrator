# auto-dev

Local supervisor-worker automation for coding agents.

## Commands

```powershell
auto-dev status
auto-dev plan "your task"
auto-dev run "your task"
auto-dev review
```

The MVP uses only Python standard library orchestration and Codex CLI as the first worker provider.

